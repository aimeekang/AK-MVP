const db = require('../database/db.js');

const getTicks = (req) => {

};

const postTick = (req) => {

};

const updateTick = (req) => {

};

module.exports = {
  getTicks,
  postTick,
  updateTick
};
