const db = require('../database/db.js');

const getTicks = (req, res) => {
  res.send('Hello from models!');
};

const postTick = (req) => {

};

const updateTick = (req) => {

};

module.exports = {
  getTicks,
  postTick,
  updateTick
};
